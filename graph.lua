StringGraph = {}

function initGraph(nodes)
end

-- each pair of nails has 4 tangent lines,
-- this function gives us the points to define the one we want
-- 
function computeEdge(a, b, aOrientation, bOrientation)
end

-- return a pseudo-random neighbor of the given state
function neighbor(state)
end